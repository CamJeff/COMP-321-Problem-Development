# First line: two integers M and N
int int

# Second line: space-separated topics (at least 1)
tokens

# Then N lines, each with:
# id (int), points (int), difficulty (int), topic (string), length (int)
repeat N:
    int int int token int